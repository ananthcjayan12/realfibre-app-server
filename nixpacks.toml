# Nixpacks configuration to install system dependencies for ReportLab
[languages.python]
version = "3.11"

[phases.setup]
aptPkgs = [
  "libfreetype6-dev",
  "libjpeg-dev",
  "zlib1g-dev",
  "pkg-config"
] 